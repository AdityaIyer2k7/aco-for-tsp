# aco-for-tsp
A modified version of the Ant Colony Optimization used to solve the Travelling Salesman Problem.

`tester.py` is the file of interest here. It runs the actual code and logs the data into an output file. This file is later converted into JSON data by `purify.py`.

`graphs.py` uses the data to generate a graph using the `matplotlib` plotting library

